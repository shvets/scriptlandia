1. Compile sources to be instrumented

>mvn compile  (or >compile.bat )

2. Run instrumented classes

>start mvn package (or >run.bat)

3. Modify sources somehow (strings to be displayed).

4. Compile sources again

>mvn compile (or >compile.bat)
