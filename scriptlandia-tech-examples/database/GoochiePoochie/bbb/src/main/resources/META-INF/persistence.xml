<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="http://java.sun.com/xml/ns/persistence"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://java.sun.com/xml/ns/persistence 
			http://java.sun.com/xml/ns/persistence/persistence_1_0.xsd"
    version="1.0">

    <persistence-unit name="jpa-101" transaction-type="RESOURCE_LOCAL">
        <provider>org.hibernate.ejb.HibernatePersistence</provider>

        <class>aaa.Forum</class>
	<!-- for portability reasons, you should list any and all entities created here, e.g. -->
	<!-- <class>aaa.EntityName</class> -->

        <properties>
            <!-- Hibernate settings -->
            <property name="hibernate.connection.driver_class"
                      value="${jdbc.driverClassName}" />
            <property name="hibernate.connection.url"
                      value="${jdbc.url}" />
            <property name="hibernate.connection.username"
                      value="${jdbc.username}" />
            <property name="hibernate.connection.password"
                      value="${jdbc.password}" />
            <property name="hibernate.dialect"
                      value="${hibernate.dialect}" />
	    
	    <!-- you may want to change this provider in production -->
            <property name="hibernate.cache.provider_class"
                      value="org.hibernate.cache.HashtableCacheProvider" />
	    
            <!-- Print SQL to stdout -->
            <property name="hibernate.show_sql" value="${hibernate.show_sql}" />
	    <!-- if you want to use Hibernate's auto schema generation, uncomment the following line -->
            <!-- property name="hibernate.hbm2ddl.auto" value="${hibernate.hbm2ddl.auto}" /-->
        </properties>
    </persistence-unit>
</persistence>

<!-- $Id: persistence.xml,v 1.3 2006/11/01 06:47:44 chrismaki Exp $ -->
