// unregisterExtension.bsh

String jdicVersion = System.getProperty("jdic.version");

String jdicLib = repositoryHome + "/org/jdesktop/jdic/" + jdicVersion + "/jdic-" + jdicVersion + ".jar";

addClassPath(jdicLib);

source("ext-installer.bsh");

new Installer().unregisterExtension();

