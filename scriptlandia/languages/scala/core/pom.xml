<?xml version="1.0"?>

<project>
  <modelVersion>4.0.0</modelVersion>
  <groupId>org.sf.scriptlandia</groupId>
  <artifactId>scala</artifactId>
  <version>2.2.2</version>
  <packaging>pom</packaging>

  <name>Scriptlandia Scala Module</name>
  <description>Scriptlandia Scala Module</description>

  <properties>
    <scala.version>2.4.0</scala.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>scala</groupId>
      <artifactId>scala-library</artifactId>
      <version>${scala.version}</version>
    </dependency>

    <dependency>
      <groupId>scala</groupId>
      <artifactId>scala-compiler</artifactId>
      <version>${scala.version}</version>
    </dependency>

    <dependency>
      <groupId>scala</groupId>
      <artifactId>scala-actors</artifactId>
      <version>${scala.version}</version>
    </dependency>

    <dependency>
      <groupId>scala</groupId>
      <artifactId>scala-dbc</artifactId>
      <version>${scala.version}</version>
    </dependency>

    <dependency>
      <groupId>scala</groupId>
      <artifactId>scala-decoder</artifactId>
      <version>${scala.version}</version>
    </dependency>
  </dependencies>

</project>
